<!-- Plugin Configuration File. Read more: https://plugins.jetbrains.com/docs/intellij/plugin-configuration-file.html -->
<idea-plugin>
    <id>dev.gaphunter.prometheusmetricnamingcompanion</id>
    <name>Prometheus Metric Naming Companion</name>
    <vendor>Gap Hunter Labs</vendor>
    <description><![CDATA[
        <p>Warning icon on any Java/Kotlin metric name literal that
        violates Prometheus's own documented naming conventions
        (prometheus.io/docs/practices/naming) -- recognizes the two
        most common real registration shapes: the Prometheus Java
        client's <code>Counter.build("name", "help")</code> and
        Micrometer's <code>Counter.builder("name")</code> (same for
        Gauge/Histogram/Summary/Timer).</p>
        <p>Checks: the name should be lowercase snake_case, a
        counter's name should end in <code>_total</code>, and a
        Histogram/Summary's name should never manually carry
        <code>_bucket</code>/<code>_count</code>/<code>_sum</code> (the
        client library appends those itself when exposing the metric).
        All three cited directly in Prometheus's own naming
        documentation.</p>
        <p><b>100% static PSI analysis</b> -- matches by simple class/
        method name only, works whether the real Prometheus/Micrometer
        jar is on the classpath or not. Java and Kotlin.</p>
        <p>Stated honestly: only checks the name literal passed
        directly to <code>build(...)</code>/<code>builder(...)</code> --
        the Prometheus client's alternate chained form
        (<code>Counter.build().name("x")</code>) isn't covered yet.</p>
        <p>Free. No license nags, no telemetry, no network calls.</p>
    ]]></description>

    <depends>com.intellij.modules.platform</depends>
    <depends>com.intellij.java</depends>
    <depends>org.jetbrains.kotlin</depends>

    <extensions defaultExtensionNs="org.jetbrains.kotlin">
        <supportsKotlinPluginMode supportsK1="true" supportsK2="true"/>
    </extensions>

    <extensions defaultExtensionNs="com.intellij">
        <codeInsight.lineMarkerProvider language="JAVA"
            implementationClass="dev.gaphunter.prometheusmetricnamingcompanion.gutter.MetricNamingLineMarkerProvider"/>
        <codeInsight.lineMarkerProvider language="kotlin"
            implementationClass="dev.gaphunter.prometheusmetricnamingcompanion.gutter.MetricNamingLineMarkerProvider"/>
        <notificationGroup id="Prometheus Metric Naming Companion" displayType="BALLOON"/>
    </extensions>
</idea-plugin>
